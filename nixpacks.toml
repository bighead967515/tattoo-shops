# Railway Nixpacks build configuration
# Ensures the correct Node.js and pnpm versions are used during CI/CD

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "pnpm run start"
